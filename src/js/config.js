/**
 * Threshold for detecting touch movement.
 * @type {number}
 */
export const TOUCH_MOVE_THRESHOLD = 50;
/**
 * Time interval for auto-changing slides.
 * @type {number}
 */
export const SLIDER_AUTO_CHANGE_TIME = 3000;
/**
 * When the user scrolls down beyond this threshold, specific styling changes
 * are applied to the header to enhance the user experience, such as adjusting
 * background color and hiding certain elements.
 *
 * The numeric value representing
 * the pixel distance from the top of the page, beyond which scrolling effects
 * are triggered in the header.
 * @type {number}
 */
export const HEADER_SCROLL_THRESHOLD = 84;
